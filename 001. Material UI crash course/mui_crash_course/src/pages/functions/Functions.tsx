import { Typography } from "@mui/material";
import React from "react";

const Functions = () => {
  return (
    <div>
      <Typography variant="h2">Functionees</Typography>
    </div>
  );
};

export default Functions;
