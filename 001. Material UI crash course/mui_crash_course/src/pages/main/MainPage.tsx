import { Typography } from "@mui/material";
import React from "react";

const MainPage = () => {
  return (
    <div>
      <Typography variant="h1"> Helloo. Main page</Typography>
    </div>
  );
};

export default MainPage;
